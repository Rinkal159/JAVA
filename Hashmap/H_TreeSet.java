//^ keys are SORTED */
//^ Time complexity changes: put, remove and get - O(logn) */
//^ NULL values are NOT allowed 
        // because it sorts the code, so it need comparison, and NULL values    can't be compared with other values 
//^ Performance: TS < HS

package Hashmap;

import java.util.TreeSet;

public class H_TreeSet {
    public static void main(String[] args) {
        TreeSet<String> ts = new TreeSet<>();
        // all the operations are same as HashSet
    }
}
